# คู่มือติดตั้งระบบสแกนใบหน้าเข้า-ออกงาน

ระบบนี้ทำงานด้วย **Google Apps Script** ล้วน ๆ (ไม่ต้องเช่าเซิร์ฟเวอร์ ไม่ต้องสร้าง Service Account) ใช้บัญชี Google ของคุณเชื่อม Drive และ Sheet โดยตรง

---

## ภาพรวมระบบ
1. รูปใบหน้าพนักงาน → เก็บใน **Google Drive** (โฟลเดอร์เดียว)
2. ผลสแกนเข้า-ออกงาน → บันทึกลง **Google Sheet**
3. หน้าเว็บสแกนหน้า → รันอยู่ใน **Google Apps Script** เปิดผ่านเบราว์เซอร์บนมือถือ/แท็บเล็ตที่จุดสแกน

---

## ขั้นตอนที่ 1: เตรียมโฟลเดอร์รูปพนักงานใน Drive

1. เข้าโฟลเดอร์รูปพนักงานที่คุณมีอยู่แล้วใน Google Drive
2. **ตรวจสอบชื่อไฟล์** — ระบบจะอ่านรหัสพนักงาน/ชื่อจากชื่อไฟล์ ให้ตั้งชื่อไฟล์ในรูปแบบ:
   ```
   รหัสพนักงาน_ชื่อพนักงาน.jpg
   ```
   ตัวอย่าง: `EMP001_สมชาย ใจดี.jpg`, `EMP002_สมหญิง รักงาน.jpg`
   (ถ้าไฟล์ไม่มี "_" ระบบจะใช้ชื่อไฟล์ทั้งหมดเป็นรหัสพนักงาน)
3. แต่ละคนควรมี **1 รูปที่เห็นหน้าชัดเจน ไม่ใส่แว่นกันแดด/หน้ากาก** แสงสว่างพอ หันหน้าตรง
4. คลิกขวาที่โฟลเดอร์ → **รับลิงก์ (Get link)** → คัดลอกลิงก์ จะได้ Folder ID ส่วนที่อยู่หลัง `/folders/` เช่น
   ```
   https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz
   ```
   Folder ID คือ `1AbCdEfGhIjKlMnOpQrStUvWxYz` → เก็บไว้ใช้ในขั้นตอนที่ 3

---

## ขั้นตอนที่ 2: สร้าง Google Sheet สำหรับบันทึกเวลา

1. ไปที่ [sheets.google.com](https://sheets.google.com) → สร้างสเปรดชีตใหม่
2. ตั้งชื่อไฟล์ เช่น "ระบบสแกนหน้าเข้าออกงาน"
3. ไม่ต้องสร้างคอลัมน์อะไรเอง ระบบจะสร้างชีตชื่อ `Attendance` และหัวตารางให้อัตโนมัติในการสแกนครั้งแรก

---

## ขั้นตอนที่ 3: ติดตั้งโค้ด Apps Script

1. ในสเปรดชีตที่สร้างไว้ ไปที่เมนู **ส่วนขยาย (Extensions) → Apps Script**
2. จะเห็นไฟล์ `Code.gs` เปล่า ๆ อยู่ → **ลบโค้ดเดิมทั้งหมดออก** แล้ววางโค้ดจากไฟล์ `Code.gs` ที่ให้มาแทน
3. แก้บรรทัดนี้ในโค้ดให้เป็น Folder ID ของคุณจากขั้นตอนที่ 1:
   ```javascript
   const DRIVE_FOLDER_ID = 'ใส่_FOLDER_ID_ของคุณตรงนี้';
   ```
4. คลิก **ไฟล์ (File) → New → HTML** (ไอคอนรูปหน้าจอ) ตั้งชื่อไฟล์ว่า `Index` (ระบบจะเติม `.html` ให้เอง — สำคัญมาก: ต้องชื่อ `Index` ให้ตรงกับที่โค้ดเรียกใช้)
5. ลบโค้ดเปล่าในไฟล์ `Index.html` ออก แล้ววางโค้ดทั้งหมดจากไฟล์ `Index.html` ที่ให้มาแทน
6. กด **บันทึก (Ctrl+S)** ทั้งสองไฟล์

---

## ขั้นตอนที่ 4: Deploy เป็นเว็บแอป

1. มุมขวาบน คลิก **Deploy (ปรับใช้) → New deployment**
2. คลิกไอคอนเฟือง ⚙️ ข้าง "Select type" → เลือก **Web app**
3. ตั้งค่า:
   - **Execute as**: Me (บัญชีของคุณ)
   - **Who has access**: เลือก **Anyone** (ถ้าอยากให้ทุกคนที่มีลิงก์เข้าได้โดยไม่ต้อง login) หรือ **Anyone within [องค์กรของคุณ]** ถ้าใช้ Google Workspace ขององค์กรและอยากจำกัดเฉพาะคนในองค์กร
4. คลิก **Deploy**
5. ครั้งแรกจะมีหน้าต่างขอสิทธิ์ (Authorize access) → เลือกบัญชี Google ของคุณ → กด **Advanced** → **Go to [ชื่อโปรเจกต์] (unsafe)** → **Allow** (ปกติของ Apps Script ที่เขียนเอง ไม่ใช่ไวรัส เป็นเพราะ Google ยังไม่ได้ verify แอปที่สร้างเอง)
6. จะได้ **Web app URL** เช่น `https://script.google.com/macros/s/xxxxx/exec` → นี่คือลิงก์สแกนหน้าของคุณ เก็บไว้/บันทึกเป็น Bookmark ที่จุดสแกน

---

## ขั้นตอนที่ 5: ทดสอบใช้งาน

1. เปิดลิงก์ Web app URL บนมือถือ/แท็บเล็ตที่จะวางไว้หน้างาน
2. อนุญาตให้เว็บเข้าถึงกล้อง (Allow camera)
3. รอระบบโหลดรูปพนักงาน (ครั้งแรกอาจช้าหน่อยถ้ามีพนักงานเยอะ ครั้งถัดไปจะเร็วขึ้นเพราะมีแคช)
4. ให้พนักงานมองกล้อง → ระบบจะจับใบหน้า เทียบกับฐานข้อมูล แล้วบันทึก "เข้างาน" ลง Sheet อัตโนมัติ
5. สแกนอีกครั้งในวันเดียวกัน ระบบจะบันทึกเป็น "ออกงาน" ให้เองสลับกับครั้งก่อน

---

## เมื่อมีพนักงานใหม่ / เปลี่ยนรูป
เพิ่ม/แก้ไขรูปในโฟลเดอร์ Drive ตามรูปแบบชื่อไฟล์เดิมได้เลย ระบบจะตรวจพบการเปลี่ยนแปลงอัตโนมัติ (เช็คจากจำนวนไฟล์และเวลาที่แก้ไข) และโหลดข้อมูลใหม่ให้เองในครั้งถัดไปที่เปิดหน้าเว็บ

## หากแก้โค้ดภายหลัง
ทุกครั้งที่แก้ไข `Code.gs` หรือ `Index.html` ต้อง **Deploy → Manage deployments → แก้ไข (ไอคอนดินสอ) → New version → Deploy** ใหม่ ไม่งั้นลิงก์เดิมจะยังใช้โค้ดเวอร์ชันเก่าอยู่

---

## ข้อควรระวัง / ข้อจำกัด
- ความแม่นยำของการจดจำใบหน้าขึ้นกับคุณภาพรูปต้นฉบับและแสงบริเวณจุดสแกน แนะนำให้ทดสอบกับพนักงานจริงก่อนใช้งานจริงทั้งโรงงาน
- ระบบเทียบใบหน้า **ทำงานในเบราว์เซอร์ของเครื่องที่สแกน** (ไม่ได้ส่งรูปขึ้น server ภายนอกอื่นนอกจาก Google) จึงต้องใช้เบราว์เซอร์ที่รองรับกล้อง เช่น Chrome
- หากพนักงานมีจำนวนมาก (หลักร้อยขึ้นไป) การโหลดรูปครั้งแรกอาจใช้เวลาสักครู่ — เป็นเรื่องปกติ ระบบมีแคชช่วยให้ครั้งถัดไปเร็วขึ้น
- ลิงก์โมเดล AI (face-api.js) ในโค้ดอ้างอิงจาก CDN สาธารณะ หากวันใดโหลดไม่ได้ ให้เปิดไฟล์ `Index.html` แล้วเปลี่ยนค่า `MODEL_URL` ตามคอมเมนต์ในโค้ด
